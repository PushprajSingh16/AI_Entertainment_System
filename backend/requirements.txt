# ===== LIGHTWEIGHT LOCAL SETUP - NO BUILD ERRORS =====
# Works with Python 3.10, 3.11, 3.12
# All packages have pre-built wheels (no C++ compilation)

fastapi==0.109.0
uvicorn==0.27.0
python-dotenv==1.0.0
groq==0.5.0
pydantic==2.5.0
python-multipart==0.0.6
httpx==0.27.0

# ===== SIMPLE RAG (No Heavy ML) =====
# Basic embedding and vector search without transformers
nltk==3.8.1